# MongoDB Atlas Configuration
# Get your connection string from MongoDB Atlas dashboard
MONGODB_URI=mongodb+srv://username:password@cluster0.xxxxx.mongodb.net/chai-fi?retryWrites=true&w=majority
DATABASE_URL=mongodb+srv://username:password@cluster0.xxxxx.mongodb.net/chai-fi?retryWrites=true&w=majority

# Session Security
# Generate a secure random string for production
# Use: openssl rand -base64 32
SESSION_SECRET=your-super-secure-random-string-here

# Application Environment
NODE_ENV=development

# Optional: For development with local MongoDB
# Uncomment and configure if using local MongoDB instead of Atlas
# MONGODB_URI=mongodb://localhost:27017/chai-fi
# DATABASE_URL=mongodb://localhost:27017/chai-fi

# Vercel Deployment Notes:
# 1. Copy this file to .env and fill in your actual values
# 2. Never commit .env to version control
# 3. Set these same variables in Vercel dashboard for production
# 4. Use strong, unique values for production deployment
